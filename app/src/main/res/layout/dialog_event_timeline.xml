<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/card_background"
    android:backgroundTint="#FFFFFF">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp">

        <TextView
            android:id="@+id/tvTimelineDialogTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_toStartOf="@+id/btnCloseTimeline"
            android:text="Lịch trình sự kiện"
            android:textColor="#1A1C1E"
            android:textSize="18sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/btnCloseTimeline"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            app:tint="#888888" />
    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvTimelineDialog"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:scrollbars="vertical" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="16dp"
        android:padding="12dp"
        android:background="#F8F9FA"
        android:backgroundTint="#F8F9FA">

        <TextView
            android:id="@+id/tvTimelineLocation"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="📍 Địa điểm: Đang tải..."
            android:textColor="#1A1C1E"
            android:textSize="13sp"
            android:textStyle="bold"/>

        <Button
            android:id="@+id/btnViewEventDetail"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="Chi tiết"
            android:textSize="12sp"
            android:paddingHorizontal="12dp"
            android:backgroundTint="#185FA5"
            android:textColor="#FFFFFF"
            app:cornerRadius="18dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"/>
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="* Mốc thời gian màu đỏ là giai đoạn đang diễn ra."
        android:textColor="#E53935"
        android:textSize="12sp"
        android:textStyle="italic"
        android:gravity="center" />

</LinearLayout>
